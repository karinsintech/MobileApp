KARINS_API_URL=https://api.karins.in/api
# Release defaults to blocking rooted/jailbroken devices (MM-09).
# Dev builds default OFF so emulators work; set true to test the gate locally.
KARINS_BLOCK_ON_ROOT=false
